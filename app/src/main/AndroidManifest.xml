<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.geek.parvaneh">

    <!-- To auto-complete the email text field in activity_loginogin form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />

    <application
        android:name=".CalligraphyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".documentActivity" />
        <activity android:name=".SportPlansActivity" />
        <activity android:name=".FoodPlanActivity" />
        <activity android:name=".AbilityActivity" />
        <activity android:name=".SportTestActivity" />
        <activity android:name=".TerminologyActivity" />
        <activity android:name=".MassageActivity" />
        <activity android:name=".BodyBuildActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".ForgetActivity" />
        <activity android:name=".NewSportPlanActivity" />
        <activity android:name=".Chang_Document" />
        <activity android:name=".TerminologyItemActivity"></activity>
    </application>

</manifest>