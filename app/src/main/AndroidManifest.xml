<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.geek.parvaneh">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Intro"/>
        <activity android:name=".MenuActivity" />
        <activity android:name=".documentActivity" />
        <activity android:name=".SportPlan" />
        <activity android:name=".FoodPlan" />
        <activity android:name=".Ability" />
        <activity android:name=".SportTest" />
        <activity android:name=".Terminology" />
        <activity android:name=".Massage" />
        <activity android:name=".BodyBuild" />
    </application>

</manifest>